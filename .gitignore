.env
__pycache__/
*.pyc
.venv/
venv/
.DS_Store
*.log

# Gabarito do analista (dados/IP — não versionar; repo é público)
data/labeled/*.pdf
data/labeled/*.xlsx
data/labeled/*.xlsm
data/labeled/*.csv

# Segredos de sessão/login — rede de segurança caso COOKIES_DIR aponte pra dentro do repo
*_credentials.json
*_state.json
*_secret.txt
cookies/
